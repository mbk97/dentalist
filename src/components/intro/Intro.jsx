import React from "react";
import { IntroContent, IntroText, Line } from "./style";

const Intro = () => {
  return (
    <IntroContent>
      <IntroText>
        Dentalist is re-imagining what the dental experience can be and proudly
        setting a new standard for patient care.
      </IntroText>
      <Line />
    </IntroContent>
  );
};

export default Intro;
